apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.analytics.name }}
spec:
  selector:
    app: {{ .Values.services.analytics.name }}
  ports:
    - port: {{ .Values.services.analytics.port }}
      targetPort: {{ .Values.services.analytics.port }}
  type: ClusterIP

